---
interface Props {
  icon: string;
  title: string;
  content: string;
}

const { icon, title, content } = Astro.props;

const iconMap: Record<string, string> = {
  efficiency: '⚡',
  design: '🎨',
  innovation: '🚀',
};
---

<div class="bg-slate-800/50 backdrop-blur rounded-xl p-6 border border-slate-700/50 hover:border-primary/50 transition-colors">
  <div class="text-3xl mb-3">{iconMap[icon.toLowerCase()] || '✨'}</div>
  <h3 class="font-heading font-semibold text-stone-300 text-lg mb-2">{title}</h3>
  <p class="text-sm text-neutral-400 leading-relaxed">{content}</p>
</div>
